apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: customer-support
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
      - name: frontend
        image: mudasir18/customer-support-frontend:latest
        ports:
        - containerPort: 80
